apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: nextcloud-aio
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/nextcloud-aio: "true"
  podSelector:
    matchLabels:
      io.kompose.network/nextcloud-aio: "true"
