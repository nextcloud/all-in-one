<html lang="en">
    <head>
        <title>AIO</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
        <link rel="stylesheet" href="style.css">
        <link rel="stylesheet" href="logs.css">
        <link rel="icon" href="img/favicon.png">
    </head>
    <body data-container-id="{{ id }}">
        <div id="floating-box">
            <div class="loader"></div>
            <div id="autoloading-box">
                <div>
                    Automatic loading of new log data is
                    <span id="autoloading-status">enabled</span>.
                </div>
                <button id="autoloading-control">Disable</button>
            </div>
        </div>
        <pre>{{ logContent }}</pre>
        <script src="log-load.js?v1" onerror="document.getElementById('autoloading-box').textContent='Error loading the JS script. Check if your ad-blocker is interfering';"></script>
    </body>
</html>
